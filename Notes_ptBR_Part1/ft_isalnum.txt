#include "libft.h"

int	ft_isalnum(int c) // Verifica se é alpha ou numero
{
	if ((c >= 'A' && c <= 'Z') // Se maiusculo
		|| (c >= 'a' && c <= 'z') // Se minusculo
		|| (c >= '0' && c <= '9')) // Se numero
		return (1); // Retorna 1
	return (0); // Se não, retorna 0
}
